{
  "name": "kpa-welfare-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Karnataka Photography Association (KPA) Welfare Management System Monorepo",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "web": "npm run dev --prefix apps/web",
    "web:build": "npm run build --prefix apps/web",
    "web:preview": "npm run preview --prefix apps/web",
    "mobile": "npm run start --prefix apps/mobile",
    "dev:backend": "cd backend && .venv\\Scripts\\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "dev:admin": "npm run dev --prefix apps/web -- --host 0.0.0.0 --port 5173",
    "dev:member-web": "npm run web --prefix apps/mobile",
    "dev": "concurrently --names \"BACKEND,ADMIN,MEMBER\" --prefix-colors \"cyan,green,magenta\" \"npm run dev:backend\" \"npm run dev:admin\" \"npm run dev:member-web\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
